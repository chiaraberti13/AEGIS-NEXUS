name: CI
on:
  push:
  pull_request:
permissions:
  contents: read
jobs:
  test:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-python@v5
        with:
          python-version: '3.12'
      - run: python -m pip install -e '.[dev]'
      - run: python -m compileall -q src
      - run: node --check src/aegis_nexus/static/app.js
      - run: node --check src/aegis_nexus/static/i18n.js
      - run: pytest -q
      - run: AEGIS_SSH_SENSOR_API_KEY=ci-ssh-secret AEGIS_WEB_SENSOR_API_KEY=ci-web-secret AEGIS_LEGACY_SENSOR_API_KEY=ci-legacy-secret AEGIS_OPERATOR_API_KEY=ci-operator-secret docker compose config -q
      - run: AEGIS_SSH_SENSOR_API_KEY=ci-ssh-secret AEGIS_WEB_SENSOR_API_KEY=ci-web-secret AEGIS_LEGACY_SENSOR_API_KEY=ci-legacy-secret AEGIS_OPERATOR_API_KEY=ci-operator-secret docker compose -f docker-compose.yml -f docker-compose.geoip.yml config -q
      - run: AEGIS_SSH_SENSOR_API_KEY=ci-ssh-secret AEGIS_WEB_SENSOR_API_KEY=ci-web-secret AEGIS_LEGACY_SENSOR_API_KEY=ci-legacy-secret AEGIS_OPERATOR_API_KEY=ci-operator-secret docker compose -f docker-compose.yml -f docker-compose.threat-context.yml config -q
      - run: docker build -t aegis-nexus:ci .
